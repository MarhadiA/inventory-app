<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory App</title>
     <!-- <link rel="stylesheet" href="/css/style.css"> -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        /* CSS Sederhana untuk tampilan */
        body { font-family: Arial, sans-serif; margin: 5px; background-color: #f4f4f4; }
        .container { max-width: 100%; margin: auto; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
        h1, h2 { color: #333; }
        .navbar { background-color: #333; overflow: hidden; margin-bottom: 20px; border-radius: 5px;}
        .navbar a { float: left; display: block; color: white; text-align: center; padding: 14px 20px; text-decoration: none; }
        .navbar a:hover { background-color: #ddd; color: black; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .btn {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 8px 12px;
            border-radius: 5px;
            text-decoration: none;
            margin-right: 5px;
            border: none;
            cursor: pointer;
        }
        .btn-add { background-color: #28a745; }
        .btn-edit { background-color: #ffc107; color: #333;}
        .btn-delete { background-color: #dc3545; }
        .btn:hover { opacity: 0.9; }
        form { margin-top: 20px; background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 0 5px rgba(0,0,0,0.05); }
        form div { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; }
        input[type="text"], input[type="number"], input[type="date"], textarea, select {
            width: calc(100% - 22px);
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        textarea { resize: vertical; min-height: 80px; }
        .error-message { color: red; background-color: #ffe5e5; border: 1px solid red; padding: 10px; border-radius: 5px; margin-top: 10px; }
        .error-message ul { margin: 0; padding-left: 20px; }
        /* Tambahkan CSS ini */
        .action-buttons {
            display: flex;
            gap: 5px; /* Memberi sedikit jarak antar tombol */
            align-items: center; /* Menyelaraskan tombol secara vertikal di tengah */
            justify-content: flex-start; /* Menyelaraskan tombol ke kiri */
            height: 100%; /* Pastikan kontainer mengambil tinggi penuh sel */
        }
        .btn {
            /* Pertahankan properti btn yang sudah ada */
            display: inline-block; /* Pastikan tombol mengambil lebar sesuai konten */
            vertical-align: middle; /* Pastikan penyelarasan vertikal di tengah */
     
}
    </style>
</head>
<body>
    <div class="container">
        <div class="navbar">
             <a href="/dashboard">Dashboard</a> 
             <a href="/items">Items</a>
            <a href="/categories">Categories</a>
             <a href="/transactions">Transactions</a>
              <a href="/reports">Reports</a>
             <% if (user) { %> <a href="/auth/logout" class="btn-danger" style="float: right;">Logout (<%= user.username %>)</a>
                <% } else { %>
                    <a href="/auth/login" style="float: right;">Login</a>
            <% } %>
        </div>